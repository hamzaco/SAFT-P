# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Numba cache
*.nbc
*.nbi

# OS
.DS_Store
Thumbs.db

# Large archive (kept locally, not pushed)
Archive.zip

# Runtime scratch / caches produced by scans
cache/
build_*/
*.mmap

# Cleanup holding area — never publish
_removed_2026-09-03/

# LaTeX build products
*.aux
*.log
*.fls
*.fdb_latexmk
*.synctex.gz
*.out
*.bbl
*.blg

# Submission bundle — manuscript, SI and referee response: keep local, never publish
corrected_submission_*/
